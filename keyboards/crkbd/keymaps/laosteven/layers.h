#pragma once

enum layers {
    _QWERTY,
    _GAMING,
    _MAC,
    _LOWER,
    _RAISE,
    _ADJUST
};

enum custom_keycodes {
    RGB_TOG = QK_USER,
    RGB_MOD,
    RGB_HUI,
    RGB_HUD,
    RGB_SAI,
    RGB_SAD,
    RGB_VAI,
    RGB_VAD,
};
